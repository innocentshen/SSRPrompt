# PromptGo

一个现代化的 AI Prompt 开发和评测平台，帮助开发者更高效地开发、测试和管理 AI Prompts。

## 功能特性

- **Prompt 开发** - 可视化界面开发和管理 AI Prompts，支持变量、多轮对话
- **评测中心** - 对 Prompts 进行系统化评测和对比，支持自定义评价标准
- **历史记录** - 追踪和查看 Prompt 执行历史，包含 Token 消耗统计
- **多数据库支持** - 支持 Supabase（云端）和 MySQL（自建）两种数据库
- **前端配置** - 无需修改代码，直接在设置页面配置数据库连接
- **主题切换** - 支持明暗主题切换
- **访问控制** - 密码保护确保数据安全

## 技术栈

### 前端
- **框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **样式**: Tailwind CSS 3
- **UI 组件**: 自定义组件库 + Lucide React 图标
- **状态管理**: React Context API

### 后端
- **框架**: Express.js + TypeScript
- **数据库**: MySQL 2 / Supabase
- **开发工具**: tsx + nodemon

## 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9
- MySQL 数据库（可选，也可使用 Supabase）

### 安装依赖

```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd server && npm install
```

### 启动项目

#### 同时启动前端和后端（推荐）

```bash
npm run dev:all
```

#### 分别启动

```bash
# 启动前端开发服务器
npm run dev

# 启动后端服务器
npm run dev:server
```

### 访问应用

- **前端**: http://localhost:5173
- **后端 API**: http://localhost:3001

默认密码：`admin123`

## 数据库配置

项目支持两种数据库方案，可以在应用的 **设置 > 数据库配置** 页面直接配置，无需修改代码。

### 方案 1：Supabase（云端，推荐快速开始）

**优点**：
- ✅ 免费额度：500MB 数据库 + 5GB 带宽/月
- ✅ 零配置，无需本地数据库
- ✅ 自动备份和高可用
- ✅ 提供可视化管理界面

**配置步骤**：
1. 在 [supabase.com](https://supabase.com) 创建免费项目
2. 从 Settings > API 获取 Project URL 和 Anon Key
3. 在应用设置页面选择 Supabase，填入连接信息
4. 点击 **初始化表结构** 按钮，复制 SQL 到 Supabase SQL Editor 执行
5. 点击 **测试连接**，成功后保存配置

**详细配置指南**：📖 [SUPABASE.md](./SUPABASE.md)

### 方案 2：本地 MySQL（推荐生产环境和私有部署）

**优点**：
- ✅ 完全控制数据
- ✅ 无带宽限制
- ✅ 适合企业内网部署

**配置步骤**：
1. 确保后端服务已启动：`npm run dev:all`
2. 在应用设置页面选择 MySQL，填入连接信息
3. 点击 **测试连接** 验证配置
4. 点击 **初始化表结构** 创建数据表
5. 保存配置

**详细部署指南**：📖 [DEPLOYMENT.md](./DEPLOYMENT.md)

### 切换数据库

- 两种数据库的配置信息会独立保存
- 切换数据库类型后，之前配置的连接信息会自动加载
- 两种数据库的数据是完全隔离的，不会自动同步

## 项目结构

```
.
├── src/                      # 前端源码
│   ├── components/          # React 组件
│   │   ├── Settings/       # 设置相关组件
│   │   │   ├── DatabaseSettings.tsx    # 数据库配置
│   │   │   └── SupabaseInitModal.tsx   # Supabase 初始化弹窗
│   │   └── ui/             # 通用 UI 组件
│   ├── contexts/           # Context 状态管理
│   ├── lib/                # 工具库
│   │   └── database/       # 数据库抽象层
│   │       ├── index.ts              # 数据库初始化
│   │       ├── types.ts              # 类型定义
│   │       ├── supabase-adapter.ts   # Supabase 适配器
│   │       ├── mysql-adapter.ts      # MySQL 适配器
│   │       └── supabase-init-sql.ts  # Supabase 建表 SQL
│   ├── pages/             # 页面组件
│   │   ├── PromptsPage.tsx    # Prompt 开发页面
│   │   ├── EvaluationPage.tsx # 评测中心页面
│   │   ├── TracesPage.tsx     # 历史记录页面
│   │   └── SettingsPage.tsx   # 设置页面
│   ├── types/             # TypeScript 类型定义
│   └── App.tsx            # 根组件
├── server/                  # 后端源码
│   ├── src/
│   │   ├── middleware/    # Express 中间件
│   │   ├── routes/        # API 路由
│   │   ├── services/      # 业务逻辑
│   │   ├── types/         # 类型定义
│   │   └── utils/         # 工具函数
│   └── package.json
├── supabase/               # Supabase 配置
│   └── migrations/        # 数据库迁移文件
└── package.json
```

## 可用脚本

### 前端

```bash
npm run dev          # 启动开发服务器
npm run build        # 构建生产版本
npm run preview      # 预览生产构建
npm run lint         # 代码检查
npm run typecheck    # TypeScript 类型检查
```

### 后端

```bash
npm run dev:server    # 启动后端开发服务器
npm run build:server  # 构建后端生产版本
```

### 全栈

```bash
npm run dev:all      # 同时启动前后端开发服务器
```

## 环境变量

复制 `.env.example` 到 `.env`（可选，也可在设置页面配置）：

```env
# Supabase 配置（可选，可在设置页面配置）
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# 访问密码（默认：admin123，请在生产环境修改）
VITE_APP_PASSWORD=admin123

# MySQL 代理服务器配置
VITE_MYSQL_PROXY_URL=http://localhost:3001/api/mysql-proxy
VITE_MYSQL_PROXY_API_KEY=your_secure_api_key_here
```

## 部署

### Docker 部署（推荐）

项目提供 Docker 配置，支持一键部署：

```bash
# 使用 docker-compose 启动
docker-compose up -d
```

详细部署指南请参考 [DEPLOYMENT.md](./DEPLOYMENT.md)

### 手动部署

```bash
# 构建前端
npm run build

# 构建后端
npm run build:server

# 启动后端
cd server && npm start

# 前端静态文件部署到 Nginx/Apache 等 Web 服务器
```

## 安全提示

- 默认密码 `admin123` 仅用于开发环境，生产环境务必修改
- API Key 应使用强随机字符串
- 建议在生产环境启用 HTTPS
- 定期更新依赖包以修复安全漏洞
- Supabase Anon Key 是公开密钥，通过 RLS 策略保护数据安全

## 开发

### 代码规范

项目使用 ESLint 进行代码质量检查：

```bash
npm run lint
```

### TypeScript

项目使用 TypeScript 提供类型安全：

```bash
npm run typecheck
```

## 许可证

MIT

## 贡献

欢迎提交 Issue 和 Pull Request！
